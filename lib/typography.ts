/**
 * Figma “Section | Regular” stack (SF Pro Text for metrics / money).
 * Use the `.font-metric` class in React for all currency, counts, and numeric UI.
 *
 * SVG/Recharts tick labels need a concrete `fontFamily` string; CSS variables are not always resolved in SVG.
 */
export const FIGMA_METRIC_FONT_STACK =
  '"SF Pro Text", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif';
